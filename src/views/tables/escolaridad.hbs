{{#if success_msg}}
<div id="success-alert" class="alert-overlay alert-center fade-out">
    <div class="col-md-2">
        <div class="alert alert-success text-center">{{success_msg}}</div>
    </div>
</div>
{{/if}}
<h2>Lista de Escolaridades</h2>
<table>
    <thead>
        <tr>
            <th>ID</th>
            <th>Nivel de Escolaridad</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        {{#each escolaridad}}
        <tr>
            <td>{{this.Id_Escolaridad}}</td> <!-- Asegúrate de que los campos coincidan con la base de datos -->
            <td>{{this.Nivel_Escolaridad}}</td>
            <td>
                <a href="/escolaridad/editar/{{this.Id_Escolaridad}}">Editar</a>
                <form action="/escolaridad/eliminar/{{this.Id_Escolaridad}}" method="POST"
                    style="display:inline;">
                    <button type="submit"
                        onclick="return confirm('¿Está seguro de que desea eliminar la escolaridad?');">Eliminar</button>
                </form>
            </td>
        </tr>
        {{/each}}
    </tbody>
</table>
